# # ORM (Object Relational Mapper)
# from sqlalchemy import Column, Integer, String, DateTime, Boolean, ForiegnKey, JSON

# from sqlalchemy.sql import func

# from sqlalchemy.orm import relationship

# from violence_detection_app.app.database.database import Base

# class Video(Base):
#     __tablename__= "video_sources"

#     id = Column(Integer, primary_key=True, index=True)
#     source_name = Column(String, index=True)
#     source_path = Column(String, index=True)
#     source_location = Column(String, index=True)
